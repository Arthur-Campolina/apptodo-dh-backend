spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: false
    roperties:
      hibernate:
        format_sql: false
security:
  oauth2:
    client:
      client-id: apptododh
      client-secret: apptododh123
jwt:
  secret: MY-JWT-SECRET
  duration: 2592000

front:
  url: ${URL_FRONT:todo}